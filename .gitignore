shared/.pi/agent/sessions
shared/.pi/agent/auth.json
shared/.pi/agent/settings.json
shared/.config/nvim/lazy-lock.json
shared/.config/tmux/plugins
shared/.config/jj/repos/
shared/.config/fish/fish_variables
shared/.local/share/
shared/.local/state/
node_modules

# FP snapshot directory (isolated git repo for workdir snapshots)
.fp/
.ragrep/
opensrc/

# witness — database, WAL files, and config (all runtime artifacts)
.witness
